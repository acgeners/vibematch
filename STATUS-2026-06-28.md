# STATUS — SatorIA / VibeMatch (2026-06-28)

> Foto consolidada do projeto após revisão de **todas** as auditorias, planos e PRs das últimas
> ~2 semanas (2026-06-12 → 28). Fontes: `AUDIT_REPORT.md`, `AUDITORIA-CICLO-VIDA-DADOS.md`,
> `MAPA-DADOS-E-ROADMAP.md`, `PLANO-MESTRE-TRANSICAO-AUDITORIA-PLANO3.md` (§24m = estado autoritativo),
> cluster `PLANO3-*` / `PLANO-E1-PRODUCAO` / `HANDOFF-OTIMIZACAO-E-DIGEST`, docs de orquestração/backfill/deploy,
> e forense de git nas branches. Convenção: 🔴 alta · 🟡 média · 🟢 baixa prioridade.

---

## 0. Atualização 2026-06-30 (housekeeping + PR #26)

> Delta desde a foto de 06-28. Onde conflitar com as seções abaixo, **este bloco prevalece**.

**Shipado — PR #26 (merged, `feat/pag-titles` → `main`, squash `f781b68`):**
- **Console `/settings` reorganizado por natureza** em 4 grupos ordenados por frequência de uso:
  1. Calibração das notas · 2. Gerado por IA · 3. Fontes externas (Comix) · 4. Avançado / manutenção (recolhido).
  1 accent por grupo (fim do "rainbow"), **chips de custo/cadência** por job, **tooltips (ⓘ) por grupo**,
  card **Comix unificado** (cobertura + diagnóstico), toggles de criação reunidos em "Comportamento na criação".
- **Trava de recálculo:** quando há artefatos gerados por IA pendentes (embeddings, sinopse canônica, resumo),
  tanto "Recalibrar agora" (painel de calibração) quanto o botão de recalcular da **sidebar/banner** abrem um
  aviso (`AiPendingGuardDialog`) pro usuário seguir ou resolver antes. Action barata `getAiPendingCounts`.
- **Calibração de critérios IA** (`/settings/calibration`): confiança mín. **default 90**, filtro **"Atributo"**
  (renomeado de "Critério", mais largo/à esquerda), removida a busca por título, texto do bulk enxugado.
- **Limpeza:** removidos `revalidatePath` redundantes de `recalculateNow`/`triggerRecalcNow` (o `recalculateAll`
  já revalida por dentro). Validado no app rodando (screenshots), **0 erros de console**.
- Nota técnica: `recalculateNow` (painel) e `triggerRecalcNow` (sidebar) são **o mesmo job** (`recalculateAll`,
  single-flight, force=true) — só mudam o contrato de retorno; não há dois recálculos distintos.

**Housekeeping de branches (2026-06-30) — corrige a §3:**
- **origin agora = `main` + `feat/shadow-ranking` apenas.**
- Apagadas (mergeadas via PR, local+remota): `feat/pag-titles` (#25/#26), `perf/ai-evaluation-counts-cache-staged-filters` (#23),
  `feat/exploration` (#22 — origin apagada; **worktree/local mantidos** a pedido). PRs #22–#26 todos merjados.
- **`feat/shadow-ranking`: preservada** — decisão de descarte **segue pendente** (experimental, migration 106 não aplicada, redundante em propósito).
- **NOVO WIP real** (não estava na foto de 06-28): **`feat/review-embeddings-ranking-transparency`** (worktree `animedb`, dev na :3001).
  2 features **fora do `main`**: (a) ranking expõe pré-filtros de preferência + Alinhamento por percentil (`c39a562`);
  (b) embeddings inclui digest/resumo de reviews no texto embedado (`9c8082e`). Sem PR; **deixada como está** (não verificada na tela).
  ⇒ Invalida o "só `feat/shadow-ranking` sobrevive" da §1/§3.
- **Worktrees ativos:** `animedb-pag-obra` → `main` · `animedb` → `feat/review-embeddings-ranking-transparency` · `animedb-exploration` → `feat/exploration`.

---

## 1. Veredito em 4 linhas

1. As branches "WIP não mergeadas" eram **4 fantasmas + 1 real**: o trabalho de 4 delas já está em `main`
   (re-implementado pela onda de PRs #9–#19). Só `feat/shadow-ranking` tem código exclusivo — e mesmo esse
   é redundante em propósito. **As 4 obsoletas foram apagadas; PR #8 fechado.**
2. O projeto está **saudável na frente de dados/ciência**: Plano 3 (Interesse) concluído e **e1 em produção**,
   tags da cauda eliminadas, orquestração construída, backfills pagos feitos.
3. As pendências reais **não estão em branches** — estão em itens de auditoria nunca iniciados
   (F1 segurança, F9 dado prospectivo, staleness/refresh) e na cauda do e1.
4. O problema relatado ("perdi o mapeamento") era **dívida de housekeeping**, não de execução:
   branches-fantasma sem limpar + plano-mestre congelado antes das 2 viradas mais recentes. Ambos corrigidos aqui.

---

## 2. Estado geral por frente

| Frente | Estado | Nota |
|---|---|---|
| **Plano 3 — Interesse na Obra** | ✅ Concluído + em produção | golden-3 (n=180, 06-25) deu GO p/ digest; e1 `v3` shipado (PRs #15/#18) |
| **Inferência de tags da cauda** | ✅ Concluído | **1877** tags `ai_inferred` (custo ~$3,93), PR #14, mig 117; cauda ≤5 tags 45→3 |
| **Orquestração durável** | 🟡 Infra pronta, 1 lacuna | falta `runDigestBatch`/`planDigestBatch` (G2); lote legado ainda bypassa o gate (G3) |
| **Backfills pagos** | ✅ Núcleo feito | Lote 02 (757/757, $5,86, perfil v8) + e1 (414/758, ~$7,1) |
| **Observabilidade/Confiabilidade IA (Plano 1/2)** | ✅ **em `main`** | `lib/ai-observability/*`, `lib/ai-cache/*`, mig 107 — re-mergeados via PRs #9–#19 |
| **Auditoria (F1–F15)** | 🟡 P0 de UX/precisão feitos; segurança + validação online não | F2/F3/F4 ✅; F1/F5–F15 abertos |
| **Deploy (Fly)** | ⬜ Diferido, nada provisionado | bloqueadores técnicos + tensão de auth (§6) |

---

## 3. Branches WIP — triagem (forense de git 2026-06-28)

> ⚠️ **Desatualizado — ver §0 (2026-06-30).** Estas eram as branches da onda 06-12→28 (todas já resolvidas).
> O estado atual do origin é `main` + `feat/shadow-ranking`, e há uma **nova** WIP (`feat/review-embeddings-ranking-transparency`).

Todas saíram do merge-base **2026-06-12** e ficaram **12 PRs atrás** de `main`.

| Branch | PR | Código único nunca mergeado | Ação tomada |
|---|---|---|---|
| `feat/realtime-chrome-refresh` | #7 MERGED | 3 arquivos-precursor obsoletos | ✅ **Apagada** (local+remota) |
| `feat/ai-observability` | #8 DRAFT | mesmos 3 precursores | ✅ **PR #8 fechado + apagada** (`main` é superset) |
| `feat/ai-reliability` | — | 3 precursores + `service.ts` v19 (antigo) | ✅ **Apagada** |
| `feat/synopsis-quality-optimization` | — | 3 precursores (experimento em `.local-experiments/`) | ✅ **Apagada** (conclusão já em produção) |
| **`feat/shadow-ranking`** | — | **~15 arquivos + migration 106** | ⏸️ **Preservada** — única real; decisão pendente (§5) |

SHAs das apagadas (recuperáveis via `git reflog`): realtime `e9d0c70` · observability `614758c` · reliability `2fc5f17` · synopsis-quality `be56513`.

Demais branches vivas: `feat/digest-canonical-corpus` (PR #15 merged; WIP local pós-merge) e `feat/alinhamento-e1-tag-reviews` (PR #16 merged) — resíduos de squash-merge, não pendências.

---

## 4. Pendências reais

### 4a. Iniciado, não finalizado

| Item | O que falta | Prio |
|---|---|---|
| **F9 — validação prospectiva** (`prediction_snapshots`) | **Semeado 2026-06-28** com 11 pontos leak-free do `prediction_ledger` (1º MAE prospectivo **0,494**, 10/11 dentro de 1pt). n pequeno → cresce com uso. Bloqueia F3 (largura de tier definitiva). | 🔴 |
| **Backfill e1 do catálogo** | só **414/758** obras com previsão e1 (`v3`). Decidir lote (~$8) vs sob demanda. | 🟡 |
| **Drift do perfil** (mig 118) | indicador heurístico shipado, **proxy não validado** (não lê sinopse); logar Δ-LLM antes de auto-pular regen. | 🟡 |
| **G3 — lote legado de digest** | `consolidatePendingReviewDigests` ainda **bypassa o gate de custo**; migrar p/ orquestração ou aposentar. | 🟡 |
| **Resumos com manual externa** | re-geram só no próximo save (não retroativo). | 🟢 |

### 4b. Planejado, não iniciado

| Item | Nota | Prio |
|---|---|---|
| **Preferências livres → preditor de Interesse** | Investigação offline **CONCLUÍDA 2026-06-30** ([PLANO-MESTRE §24n](PLANO-MESTRE-TRANSICAO-AUDITORIA-PLANO3.md#24n)): formato **v3.3** decidido (calibrador de over-rating; utilidade assimétrica gems>MAE; teto promoção ♥♥♥ mantido). **Produção NÃO iniciada** — sequência aprovada: peça 2 (integração, v3.3 hardcoded) → peça 3 (backfill deferred) → peça 1 (compilador-LLM). Bloco canônico em `rules-exp-v33/result.json`. | 🟡 |
| **F1 — auth + rate-limit** (~30 server actions service-role expostas) | bloqueia deploy **público** (visitante muta dados/drena orçamento Anthropic). Ver tensão §6. | 🔴 p/ deploy |
| **G2 — `runDigestBatch`/`planDigestBatch`** | nunca construído. Era condicional a "digest vencer" — **venceu (golden-3)**. É o que falta p/ backfillar digest. | 🔴 reaberto |
| **Completar digest da cauda reviewável** (~60–130 obras) | **Medido no banco 2026-06-28: digest já cobre 481/773 = 62%** (NÃO 2% — o doc de 06-18 estava stale; o rollout do e1 backfillou via cascata tags→reviews→digest). Resta a cauda com review-mas-sem-digest; ~231 obras sem review não são backfilláveis. | 🟡 |
| **Auto-refresh pós-perfil/recalc** (F11/G4/L6) | recalc 1h/manual; Interesse stale ao mudar perfil; `taste_profile` só regenera no fluxo pago. | 🟡 |
| **F6 — Veredito IA** (`alignment_score`, LLM) — **MANTER** | **Re-medido 2026-06-28 (n=112):** ranking global fraco (Spearman 0,207, empata calc) MAS **precision@5 = 1,0 / @10 = 0,80 — o MELHOR sinal no TOPO** (expected 0,6/0,5; calc 0,8/0,4). Confirma o audit "forte só no topo" — exatamente o que um re-rank sob demanda precisa. **NÃO** de-enfatizar. Ressalva: n pequeno + precision de lista única + seleção (pool já bom, média 8,19 vs 7,81 do catálogo) ⇒ confirmar prospectivamente via F9. | 🟢 |
| **F5 — simplificar Ridge → calc** | ganho do Ridge não demonstrado (IC⊃0); simplificação de risco/código. | 🟢 |
| **F12 — refresh de dados externos estagnados** | sem política de re-fetch. | 🟢 |
| **F8 (mood duplicado) · F10 (Sonnet→Haiku) · F13 (waterfall) · F14 (lint 444/29 err) · F15 (código morto `prediction.ts`)** | hardening técnico / produto. | 🟢 |
| **Stage-1 + Interesse-via-Ridge** | testes pausados (caro × retorno marginal); reabrir só com mais labels. | 🟢 |
| **Fase 2/3 do digest** | cap dinâmico de amostragem (Fase 2); digest na Avaliação IA de atributos (Fase 3). | 🟢 |
| **Deploy Fly** | resolver-hid prod-safe (GH Action+Chrome); validar IA>60s vs idle-timeout; drop de colunas legadas `formula_config`. | 🟡 quando deployar |
| **Reaper de jobs `running` órfão** | hoje só avisa; crash trava o `dedup_key`. | 🟢 |

---

## 5. Próximos passos propostos (ordem custo → valor)

| # | Ação | Por quê | Custo |
|---|---|---|---|
| 1 | ✅ **Limpar branches + fechar PR #8** | feito nesta sessão | — |
| 2 | ✅ **Reconciliar PLANO-MESTRE** (§24m) | feito nesta sessão | — |
| 3 | **Re-prever em `v3` as ~319 obras ainda em v1/v2** + completar digest da cauda reviewável (~60–130) | digest já cobre 62%; o ganho real é trazer as obras antigas p/ o preditor com digest. `runDigestBatch` (G2) é conveniência, não bloqueio | ~$3–5 LLM |
| 4 | **Usar o app** p/ acumular `prediction_snapshots` (F9) | F9 (calibração prospectiva da nota headline) = **0 linhas**, só acumula com uso. Destrava F3 (largura de tier) e dá confirmação prospectiva leakage-free do veredito **já existente** de F5/F6 (não os cria) | $0 (só uso) |
| 5 | **Resolver tensão F1/deploy** (auth mínimo vs acesso-restrito) antes de qualquer Fly público | drenagem de orçamento Anthropic por terceiros é risco real | decisão de produto |
| 6 | **shadow-ranking**: descartar OU rebasear p/ validação prospectiva contínua | única branch única, mas propósito já coberto por §24k | sua escolha |
| 7 | **Decidir auto-refresh** (F11/G4): job noturno OU on-write de recalc/Interesse/perfil | resolve staleness sistêmica de uma vez | baixo, $0 |

**NÃO fazer agora** (ganho marginal ou IC⊃0): F5 (simplificar Ridge), F14 (lint), Stage-1/Fase-3 do digest, F8 (mood unificado). Reabrir só quando houver **mais labels** — a única alavanca que move a ciência.

---

## 6. Contradições nos planos — status de reconciliação

| # | Contradição | Status |
|---|---|---|
| 1 | `PLANO-MESTRE` defasado: "b1, sem digest / G2 dispensado / D2 adiar" vs golden-3 GO + e1 produção | ✅ **Reconciliado** (§24m + banner + ponteiros §15/§20/§24i) |
| 2 | `MAPA-DADOS` §2: glossário diz `netNameOverlap`, seção de detalhe ainda descreve fórmula antiga de 3 componentes | ⬜ **Aberto** — corrigir a seção de detalhe do MAPA |
| 3 | "~622 obras pendentes de Interesse" (06-18/19) vs Lote 02 = 757/757 (§24j) | ✅ Anotado obsoleto no §24m |
| 4 | D2 "adiar digest" vs e1 em produção usando digest | ✅ Marcado superseded (§24m) |
| 5 | tags `ai_inferred`: 831 (índice memória) vs 1877 (estado real) | ✅ Corrigido na memória |
| 6 | F1 (bloqueia deploy) vs DEPLOY-FLY (app sem auth, deploy não espera) | ⬜ **Decisão pendente** (§5 item 5) — depende de exposição pública vs restrita |
| 7 | Números flutuantes de `n`/catálogo (190/191/192; 724/734/737/757/758) | ℹ️ Por data; sempre datar — não é erro factual |

---

## 7. Migrations — estado

Presentes no repo: até **121**. **Gap proposital: 106** (`ranking_strategy_snapshots`) só existe em `feat/shadow-ranking` e **nunca foi aplicada**. Demais (105, 107–121) aplicadas à mão no SQL editor (CLI dessincronizada — ver memória `project_migration_apply_mechanism`). Destaques recentes: 117 (provenance de tags), 118 (fingerprint de drift do perfil), 119 (toggle sinopse canônica no create), 120 (`ai_eval_reviews_stale`), 121 (`synopsis_interest_skipped`).

---

## 8. Verificações empíricas + execução (2026-06-28, parte 2)

Medições reais no banco de produção (não confiar em números de docs antigos).

**Cobertura de digest — corrigida:** `review_digest` cobre **481/773 = 62%** (não 2%); previsão `v3` em **454/773**; `review_summary` 607/773. O rollout do e1 backfillou via cascata. ⇒ backfill de digest **não é gargalo**; resta cauda reviewável ~60–130.

**Alinhamento ≠ Veredito IA (são distintos):**
- **Alinhamento** = `tag_overlap_net` (determinístico; amado − 1,5×evitado; mudou no PR #16). pairAcc geral 0,695.
- **Veredito IA** = `alignment_score` (LLM, re-rank pago sob demanda). Não mudou.

**Veredito IA re-medido (n=112 com `alignment_score` + `user_score`):** ranking GLOBAL fraco (pairAcc 0,576 / Spearman 0,207, empata `calc_score`), MAS no TOPO é o **melhor sinal** — **precision@5 = 1,0 / @10 = 0,80** (vs expected 0,6/0,5; calc 0,8/0,4). Confirma o audit "forte só no topo". ⇒ **Decisão revista: MANTER** o Veredito (número + texto); o que ele faz mal (ordenar a cauda) não é o trabalho dele (re-rank sob demanda = "isto é top pra mim?"). NÃO de-enfatizar. Ressalvas: n=112, precision de lista única (alta variância), seleção (pool média 8,19 > 7,81), retrospectivo ⇒ confirmar via F9.

**F9 SEMEADO:** 11 pontos leak-free do `prediction_ledger` → `prediction_snapshots` (`prediction_context='ledger_backfill'`, reversível). **1º MAE prospectivo = 0,494** (10/11 dentro de 1pt), coerente com o CV ~0,58. n=11 não conclui — é o ponto de partida; o painel `/admin/model-metrics` agora mostra dado real.

**Pesar `ai_inferred` no Alinhamento = NO-GO empírico:** só 5,4% (33/610) dos matches do perfil vêm de tags `ai_inferred`-only; down-weight (×0,5 ou ×0) move pairAcc em ≤+0,002. Não implementado.

**Limpeza concluída:** 6 branches WIP apagadas no total (4 superseded + 2 já-mergeadas `digest-canonical-corpus`/`alinhamento-e1-tag-reviews`); só `feat/shadow-ranking` sobra. 18 docs de plano superados → `docs/archive/` (links do PLANO-MESTRE corrigidos). Raiz: 10 .md.
